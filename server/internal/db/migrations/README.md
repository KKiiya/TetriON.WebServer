# Internal Migration Notes

This folder is reserved for server-owned migration helpers and metadata.

The canonical SQL migration files currently live in the repository root `migrations/` folder.

If runtime-managed migrations are introduced, keep migration coordination code and state files in this directory.
